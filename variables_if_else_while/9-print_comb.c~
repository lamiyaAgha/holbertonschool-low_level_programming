#include <stdio.h>

int main(void)
{
    for (int i = '0'; i <= '9'; i++)
    {
        for (int j = '0'; j <= '9'; j++)
        {
            putchar(i);
            putchar(',');
            putchar(' ');
            putchar(j);
            putchar('\n');
        }
    }

    return (0);
}

